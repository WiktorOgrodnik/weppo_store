<%- include('layout-top', {static_correction: './../', categories: locals.categories}) %>
<main>
    <h3>Produkty z kategorii:</h3><br>
    <ul>
    <% for (let i of locals.products) {%> 
        <a href="../product/<%= i.product_id %>"><%= i.name %></a>
    <% } %>
    </ul>
   
</main>
<%- include('layout-bottom', {}) %>